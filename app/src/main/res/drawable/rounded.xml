<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <corners android:radius="24dp" /> <!-- Sesuaikan nilai 'radius' untuk kebulatan yang Anda inginkan -->

    <!-- Warna latar belakang -->
    <!-- #33FFFFFF adalah kode ARGB: 33 (alpha/opacity) FF FF FF (putih). Ini berarti putih dengan opasitas sekitar 20%. -->
    <solid android:color="@color/transparent" />

    <!-- Garis tepi (border) -->
    <!-- 'width' adalah ketebalan garis, 'color' adalah warna garis -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray" /> <!-- Warna abu-abu gelap untuk border -->

    <!-- Padding di dalam EditText, antara teks dan batas background -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>
