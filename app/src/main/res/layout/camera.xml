<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Camera preview -->
    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fillCenter" />

    <!-- Overlay teks di atas kamera -->
    <TextView
        android:id="@+id/overlayText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="TEMPATKAN KAMERA PADA OBJECK MATA"
        android:textAllCaps="true"
        android:textSize="18sp"
        android:gravity="center"
        android:padding="12dp"
        android:layout_marginTop="24dp"
        android:background="#66000000"  <!-- semi-transparent gelap -->
    android:textColor="#FFFFFF" />

    <!-- (opsional) Tombol tutup atau lainnya bisa ditambahkan di sini -->

</FrameLayout>
